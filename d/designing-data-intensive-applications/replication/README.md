# Replication

Replication is keeping a copy of the same data on multiple machines connected via a network.

Why?

* Reduce latency with geographic closeness
* In case of failures on another node
* Scale out machines and increase throughput

